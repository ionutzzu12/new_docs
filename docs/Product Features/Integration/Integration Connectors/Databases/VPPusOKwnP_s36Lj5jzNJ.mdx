---
title: DB - PostgreSQL SSL
slug: VPPusOKwnP_s36Lj5jzNJ
description: Review parameters for the PostgreSQL SSL connector. 
docTags: 
createdAt: Fri Dec 01 2023 20:41:11 GMT+0200 (Eastern European Standard Time)
---

Review the :Link[PostgreSQL SSL Integration Guide]{label="PostgreSQL SSL Integration Guide" spaceId="JWQMp_kvm0WsA_Fpjt1v_" docId="BKSG9HIWagS5uBQW9tTfY" version="v2" docAnchorId loadingMethod="dynamic" overridedLabel}.&#x20;

<ContentSnippet docRefId="gLOJmRmDCovD9YSiJTYh3">

</ContentSnippet>

<hint type="info">
  **Note**: When the *Create table* checkbox is not selected, the table format (columns) that you will create must still adhere to the following format.&#x20;
</hint>

```pgsql
CREATE TABLE IF NOT EXISTS "TABLE_NAME"
(
    id BIGSERIAL NOT NULL,
    record_uuid UUID NOT NULL,
    arrived_at  TIMESTAMP WITH TIME ZONE NOT NULL,
    device_id   VARCHAR(64)    NULL,
    register_id VARCHAR(64)    NULL,
    tag_name    VARCHAR(64)    NULL,
    datatype    VARCHAR(32)    NULL,
    value       VARCHAR NULL,
    success     BOOLEAN        NULL,
    PRIMARY KEY (id)
);
```

