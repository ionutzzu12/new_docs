---
title: MySQL SSL Integration Guide
slug: eMt5kewrupTl1JeHABJsy
description: Learn how to set up the MySQL with SSL connector. 
docTags: 
createdAt: Tue Jan 16 2024 22:48:35 GMT+0200 (Eastern European Standard Time)
---

Review the following guide for setting up an integration between Litmus Edge and a MySQL database using SSL authentication.&#x20;

# Before You Begin

You will need the following:

- Access to a MySQL database with SSL authentication.&#x20;
- The CA Certificate and any other required authentication parameters to access the database.&#x20;

You can use the following supported versions:

- MySQL (4.1+)
- MariaDB&#x20;
- Percona Server
- Google CloudSQL
- Sphinx (2.2.3+)

Refer to the following links to learn more:

- :Link[MySQL Docker Image]{href="https://hub.docker.com/_/mysql" newTab="true" hasDisabledNofollow="false"}
- :Link[MySQL Documentation]{href="https://dev.mysql.com/doc/" newTab="true" hasDisabledNofollow="false"}
- :Link[Configuring MySQL to Use Encrypted Connections]{href="https://dev.mysql.com/doc/refman/8.0/en/using-encrypted-connections.html" newTab="true" hasDisabledNofollow="false"}

# Step 1: Add Device

<ContentSnippet docRefId="4BJscZZuOa_vq_2mMaT9_">

</ContentSnippet>

# Step 2: Add Tags

<ContentSnippet docRefId="WypbP7NZeCWlNxyPc0CG_">

</ContentSnippet>

# Step 3: Add the DB - MySQL SSL Connector

Follow the steps to [Add a Connector](<../../Product Features/Integration/2-UCRLFSRK14ramqhVm36.mdx>) and select the **DB - MySQL SSL&#x20;**&#x70;rovider.&#x20;

Configure the following parameters.&#x20;

- **Name**: Enter a name for the connector.&#x20;
- **Hostname**: Enter the IP address to access the database.&#x20;
- **Port**: The port needed to access the database. The default value is **3306**.&#x20;
- **CA Certificate**: Paste or upload the CA certificate associated with the database.&#x20;
- **Certificate**: Paste or upload the SSL certificate.
- **Private Key**: Enter or paste the SSL private key.&#x20;
- **Username**: Enter the username to access the database.&#x20;
- **Password&#x20;**(Optional): Enter the password associated with the username.&#x20;
- **Database**: Enter the database name.&#x20;
- **Table**: Enter a name to create a new table or enter the name of an existing table.&#x20;
- **Show Mapping**: If you want to send data to a custom table, select this check box and unselect *Create table*. See :Link[Work with Tables in SQL Connectors (Create Table and Show Mapping)]{docId="MEnhPdqNbkR2MFY8Z1Ogq" label="Work with Tables in SQL Connectors (Create Table and Show Mapping)" spaceId="JWQMp_kvm0WsA_Fpjt1v_" version="v2" docAnchorId loadingMethod="dynamic" overridedLabel} to learn more. To add key/value pairs for the custom table, see :Link[Configure Key/Value Pairs]{docId="MEnhPdqNbkR2MFY8Z1Ogq" label="Work with Tables in SQL Connectors (Create Table and Show Mapping)" spaceId="JWQMp_kvm0WsA_Fpjt1v_" version="v2" docAnchorId="#configure-keyvalue-pairs" loadingMethod="dynamic" overridedLabel="Configure Key/Value Pairs"}.&#x20;
- **Create table**: If you want to send data to an existing table in the default format, or you want to create a new table in the default format, select this check box and unselect *Show Mapping*. See :Link[Work with Tables in SQL Connectors (Create Table and Show Mapping)]{docId="MEnhPdqNbkR2MFY8Z1Ogq" label="Work with Tables in SQL Connectors (Create Table and Show Mapping)" spaceId="JWQMp_kvm0WsA_Fpjt1v_" version="v2" docAnchorId loadingMethod="dynamic" overridedLabel} to learn more.&#x20;
- **Commit timeout**: Enter the transaction commit timeout in (ms).
- **Max transaction size**: Enter the maximum number of messages before a transaction is committed, regardless of timeout parameter.&#x20;
- **Bulk insert count**: To enable this option, enter the number of messages to group together and send as one bulk insert statement. Enabling this option can improve how quickly data is processed when dealing with high volumes of tags.&#x20;
- **Throttling limit**: The maximum number of messages per second to be processed. The default value is zero, which means that there is no limit.&#x20;
- **Persistent storage**: When enabled, this will cause messages to undergo a store-and-forward procedure. Messages will be stored within Litmus Edge when cloud providers are online. &#x20;
- **Queue Mode**: Select the queue mode as **lifo&#x20;**(last in first out) or **fifo&#x20;**(first in first out). Selecting **lifo&#x20;**&#x6D;eans that the last data entry is processed first, and selecting **fifo&#x20;**&#x6D;eans the first data entry is processed first.&#x20;

# Step 4: Enable the Connector

After adding the connector, click the toggle in the connector tile to enable it. &#x20;

<Image src="https://archbee-image-uploads.s3.amazonaws.com/SSUUxKZUk9bFTEPNn_6Zo/LGZNs7jWb1Fbex36zMkf0_mysqlssl.png" size="60" width="388" height="358" position="center" showCaption="false"></Image>

If you see a *Failed&#x20;*&#x73;tatus, you can review the :Link[Connector Logs]{label="Manage Connectors" spaceId docId="p4fwhKRt2q2hsjboZv6hk" version="v2" docAnchorId="#connector-logs" loadingMethod="dynamic" overridedLabel="Connector Logs" href="../../docs/Product Features/sg--6i_X4_uGD7by2g88L/A4IRTEnfEkJidIw7T_Lsd.mdx"} and relevant error messages.&#x20;

# Step 5: Create Topics for Connector

You will now need to import the tags created in Step 2 as topics for the MySQL connector. The topics will be created as outbound topics.&#x20;

<ContentSnippet docRefId="IFvGjz8wQDN5qgSojCte8">

</ContentSnippet>

After adding all required topics, navigate to the Integration overview page and ensure the connector is not disabled and still shows a CONNECTED status.&#x20;

# Step 6: Enable Topics

To enable the topics, return to the *Topics* tab and click the **Enable all topics** icon.&#x20;

<Image src="https://archbee-image-uploads.s3.amazonaws.com/SSUUxKZUk9bFTEPNn_6Zo/Iq7BWYqGWueu4US73iBl3_image.png" size="80" width="1220" height="330" caption="Enable all topics icon" position="center" showCaption="true"></Image>

# Step 7: Make MySQL Queries

You can now verify that you can view data in the MySQL database.&#x20;

Update the credentials, database name, and table name to your own specific configurations.&#x20;

**To make MySQL queries:**

1. From the MySQL terminal window, enter `mysql -u user -p` and press **ENTER**.
   `Enter password: `appears.
2. Enter your user password, and then press **ENTER**.
   *Welcome to the MySQL monitor&#xA0;*&#x61;ppears.
3. Enter `show databases;`**,&#x20;**&#x61;nd then press **ENTER**.
   The database name appears.
4. Enter `use sample;`, and then press **ENTER**.
   *Reading table information for completion of table and column names&#xA0;*&#x61;ppears in the console.
5. Enter `show tables;`, and then press **ENTER**.
   The table names for *sample* appear.

6. Enter `select * from test_table;` , and press **ENTER**.
   Data appears from the imported tags for the connector.&#x20;

